@model IEnumerable<QRCodeRedirects.Models.QRCodeRedirect>

@{
    ViewBag.Title = "Index";
}

<h2> </h2>

<p>
    @Html.ActionLink("Create New", "Create", new { id = 0 }, new { @class = "btn btn-success btn-sm" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QRCodeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WidenURL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.QRCodeId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.WidenURL)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.QRCodeId }, new { @class = "btn btn-default btn-sm" }) 
        @*@Html.ActionLink("Details", "Details", new { id = item.QRCodeId }, new { @class = "btn btn-default btn-sm" })  
        @Html.ActionLink("Delete", "Delete", new { id = item.QRCodeId }, new { @class = "btn btn-default btn-sm" })
    </td>*@
</tr>
}

</table>
